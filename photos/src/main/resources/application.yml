spring:
  application:
    name: photos

server:
  port: 8080

minio:
  url: ${MINIO_URL:http://minio:9000}
  bucket: ${MINIO_BUCKET:pitomets-images}
  access-key: ${MINIO_ACCESS_KEY:minioadmin}
  secret-key: ${MINIO_SECRET_KEY:minioadmin}

logging:
  level:
    com.pitomets.photos: INFO
