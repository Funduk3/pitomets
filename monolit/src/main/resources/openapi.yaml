openapi: 3.0.3
info:
  title: Monolit API (generated)
  version: 1.0.0
  description: Auto-generated OpenAPI spec for main DTOs and discovered endpoints
    in project.
servers:
  - url: http://localhost:8080
    description: Local server
paths:
  /register:
    post:
      summary: Register new user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserResponse'
  /login:
    post:
      summary: Login user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
  /refresh:
    post:
      summary: Refresh access token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshTokenRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
        '401':
          description: Unauthorized
  /logout:
    post:
      summary: Logout (invalidate refresh token)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshTokenRequest'
      responses:
        '200':
          description: OK
  /all:
    get:
      summary: Get all users
      responses:
        '200':
          description: List of users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/UserResponse'

components:
  schemas:
    AddressCreateRequest:
      type: object
      properties:
        userId:
          type: integer
          format: int64
        city:
          type: string
        street:
          type: string
        house:
          type: string
        flat:
          type: integer
          format: int32
      required:
        - userId
        - city
        - street
        - house
        - flat

    AddressResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        userId:
          type: integer
          format: int64
        city:
          type: string
        street:
          type: string
        house:
          type: string
        flat:
          type: integer
          format: int32
      required:
        - id
        - userId
        - city
        - street
        - house
        - flat

    BuyerProfileResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        buyerId:
          type: integer
          format: int64
      required:
        - id
        - buyerId

    FavouriteRequest:
      type: object
      properties:
        buyerProfileId:
          type: integer
          format: int64
        listingId:
          type: integer
          format: int64
      required:
        - buyerProfileId
        - listingId

    FavouriteResponse:
      type: object
      properties:
        buyerProfileId:
          type: integer
          format: int64
        listingId:
          type: integer
          format: int64
      required:
        - buyerProfileId
        - listingId

    ListingCreateRequest:
      type: object
      properties:
        sellerProfileId:
          type: integer
          format: int64
        description:
          type: string
        species:
          type: string
        breed:
          type: string
        ageMonths:
          type: integer
          format: int32
        motherId:
          type: integer
          format: int64
        fatherId:
          type: integer
          format: int64
        price:
          type: integer
          format: int32
        isArchived:
          type: boolean
      required:
        - sellerProfileId
        - isArchived

    ListingResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        sellerProfileId:
          type: integer
          format: int64
        description:
          type: string
        species:
          type: string
        breed:
          type: string
        ageMonths:
          type: integer
          format: int32
        motherId:
          type: integer
          format: int64
        fatherId:
          type: integer
          format: int64
        price:
          type: integer
          format: int32
        isArchived:
          type: boolean
      required:
        - id

    LoginRequest:
      type: object
      properties:
        fullName:
          type: string
        passwordHash:
          type: string
      required:
        - fullName
        - passwordHash

    LoginResponse:
      type: object
      properties:
        token:
          type: string
      required:
        - token

    PetCreateRequest:
      type: object
      properties:
        species:
          type: string
        breed:
          type: string
        age:
          type: integer
          format: int32
        weight:
          type: number
          format: double
        gender:
          type: string
        description:
          type: string

    PetResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        species:
          type: string
        breed:
          type: string
        age:
          type: integer
          format: int32
        weight:
          type: number
          format: double
        gender:
          type: string
        description:
          type: string
      required:
        - id

    RegisterRequest:
      type: object
      properties:
        fullName:
          type: string
        passwordHash:
          type: string
      required:
        - fullName
        - passwordHash
    ReviewCreateRequest:
      type: object
      properties:
        reviewerId:
          type: integer
          format: int64
        sellerProfileId:
          type: integer
          format: int64
        starsNumber:
          type: integer
          format: int32
        message:
          type: string
      required:
        - reviewerId
        - sellerProfileId
        - starsNumber

    ReviewResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        reviewerId:
          type: integer
          format: int64
        sellerProfileId:
          type: integer
          format: int64
        starsNumber:
          type: integer
          format: int32
        message:
          type: string
      required:
        - id
        - reviewerId
        - sellerProfileId
        - starsNumber

    SellerProfileCreateRequest:
      type: object
      properties:
        sellerId:
          type: integer
          format: int64
        shopName:
          type: string
        description:
          type: string
      required:
        - sellerId
        - shopName

    SellerProfileResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        sellerId:
          type: integer
          format: int64
        shopName:
          type: string
        description:
          type: string
        rating:
          type: integer
          format: int32
      required:
        - id
        - sellerId

    TokenResponse:
      type: object
      properties:
        accessToken:
          type: string
        refreshToken:
          type: string
        tokenType:
          type: string
      required:
        - accessToken
        - refreshToken
        - tokenType

    UserResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        fullName:
          type: string
      required:
        - id
        - fullName

    RefreshTokenRequest:
      type: object
      properties:
        refreshToken:
          type: string
      required:
        - refreshToken