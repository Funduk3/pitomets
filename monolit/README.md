docker-compose up --build

./gradlew clean build

БД postgres

url: jdbc:postgresql://localhost:5432/pitomets

username: user

password: password

prometheus: http://host.docker.internal:9090

minio: admin admin
